/// Serviço responsável por renovar o token de autenticação
class AuthRefreshService {
  /// Retorna um novo token ou null caso não seja possível renovar
  Future<String?> refreshToken() async {
    // Quando existir, este método será implementado
    return null;
  }
}
